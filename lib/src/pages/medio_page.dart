import 'package:flutter/material.dart';

class MedioPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Medio Page'),
      ),
    );
  }
}