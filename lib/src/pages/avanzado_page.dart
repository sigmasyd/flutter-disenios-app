import 'package:flutter/material.dart';

class AvanzadoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Avanzado Page'),
      ),
    );
  }
}